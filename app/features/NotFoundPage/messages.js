/*
 * NotFoundPage Messages
 *
 * This contains all the text for the NotFoundPage feature.
 *
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.features.NotFoundPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NotFoundPage feature!',
  },
});
